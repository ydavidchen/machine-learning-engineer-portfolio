from setuptools import setup

setup(
    name = 'custom-distributions',
    version = '0.1',
    description = 'Custom Distributions',
    packages = ['custom-distributions'],
    zip_safe = False
);
