from setuptools import setup

setup(
    name = "distributions",
    version = "0.1",
    descriprion = "Gaussian and other common distributions",
    packages = ["distributions"],
    zip_safe = False
)